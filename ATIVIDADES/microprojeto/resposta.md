Q01 = 
<head>
  <script src="script.js" defer></script>
</head>

Q02=
I – B, II – C, III – A

Q03=
O método utilizado é classList.add(), e o atributo classList é uma lista de classes que permite manipular dinamicamente as classes CSS de um elemento.

Q04=
O método createElement() cria um novo elemento HTML e recebe como argumento o nome da tag do elemento que será criado.

Q05=
b — innerText retorna ou define apenas o texto visível ao usuário, enquanto innerHTML retorna ou define o conteúdo HTML do elemento, incluindo tags.

Q06=
O evento foi definido com o atributo .onclick, que recebe uma função a ser executada quando o usuário clicar no elemento.

Q07=
O método addEventListener() serve para registrar uma função que será executada quando um evento específico ocorrer.
Ele recebe dois parâmetros: (O tipo de evento (ex: "click", "submit")./ A função que será chamada quando o evento acontecer.)